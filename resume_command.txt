# ajouter fichier dans index
git add mon_fichier

# commiter les modifications
git commit -m "information a renseigner"

# commiter les fichiers deja indexer"
git commit -a -m "information a renseigner"

# historique des commits
git log

# revenir sur un commit
git chekout "num de commit"

# annuler le retour commit
git checkout master

# envoyer un nouveau repository vers github
git remote add origin  https://github.com/Schoomee/TutoGit.git

# …or push an existing repository from the command line
git remote add origin https://github.com/Schoomee/TutoGit.git
git push -u origin master

test 
